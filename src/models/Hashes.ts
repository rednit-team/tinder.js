/**
 * The general type of an value hash.
 *
 * @type ValueHash The general hash type
 */
export type ValueHash = {
  /**
   * The version of the hash
   */
  version: string;
  /**
   * The value of the hash
   */
  value: number;
}
